import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Block Reward and Halving",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
