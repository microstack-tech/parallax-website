import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Difficulty and Fork Choice",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
